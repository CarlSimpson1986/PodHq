---
description: Deploy PodHQ to Vercel
---
Run `vercel --prod` from the project root. Before deploying, run `npm run build` to check for errors. If the build fails, fix the errors before deploying. Confirm the deployment URL after success.
